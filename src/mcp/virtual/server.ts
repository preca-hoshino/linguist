// src/mcp/virtual/server.ts — 虚拟 MCP Server 聚合层（占位）

// TODO: Phase 4
// - 负责将后端多个真实 MCP Server 的工具列表聚合，并暴露统一的 MCP Server 接口
// - 负责执行 App 级别的 virtual_mcps 工具拦截和名称冲突消解

export class VirtualMcpServer {
  // 占位
}
