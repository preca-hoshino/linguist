// src/mcp/providers/base-client.ts — 抽象 MCP Client 接口（占位）

// TODO: Phase 4
// - 提供连接后端 MCP Server 的统一基类或接口标准 (如 connect, listTools, callTool)

export type AbstractMcpClient = Record<string, unknown>;
